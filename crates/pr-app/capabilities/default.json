{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 0 spike: window control only. App commands need no permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
